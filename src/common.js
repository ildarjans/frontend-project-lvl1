export const printWinGame = (name) => console.log(`Congratulations, ${name}!`);

export const printLoseGame = (name) => console.log(`Let's try again, ${name}!`);

export const printCorrect = () => console.log(`Correct!`);

export const printConditions = (condition) => console.log(condition);

export const printQuestion = (question) => console.log(`Question ${question}`);